PORT=3080
TZ=Asia/Shanghai
# 仅透明代理 fake-IP 网络按需开启，普通部署保持 false
ALLOW_DNS_FAKE_IP=false
# 所有用户合计的外部请求并发上限
MAX_OUTBOUND_REQUESTS=16
# 每个客户端 IP 每分钟的 API 请求上限
RATE_LIMIT_PER_MINUTE=180
# 直接访问或单层反向代理保持 1
TRUST_PROXY_HOPS=1
